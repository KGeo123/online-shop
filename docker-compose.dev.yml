version: "3"

services:
  client:
    container_name: online-shop-client
    build:
      context: ./client
      target: development
    volumes:
      - ./client/src:/app/src
    ports:
      - 3000:3000
    environment:
      - CHOKIDAR_USEPOLLING=true